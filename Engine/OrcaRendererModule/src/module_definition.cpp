#include "OrcaRendererModule/orca_renderer_module.h"

using namespace Engine::Extension::OrcaRendererModule;

static void RenderLoop()
{
    // foreach object: create object-related calls

    // foreach rener graph:
    // foreach pass:
    // create state switch calls (with special sort keys)

    // sort commands
    // draw
}